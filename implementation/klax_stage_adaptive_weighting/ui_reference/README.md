# UI references

`approved_probability_lab_exact.html` is the exact Probability Lab design supplied and approved by the user. Use it as the visual and interaction baseline.

The embedded July 7 data and JavaScript calculations in that standalone file are fixtures only. The production application must remove fixture fallback and render immutable backend values.

`current_signal_room_reference.png` is a current implementation screenshot supplied for context. It is not the target for the new weight panels; preserve the existing application navigation and styling while adding the requirements in `docs/06_PROBABILITY_LAB_UI.md`.
