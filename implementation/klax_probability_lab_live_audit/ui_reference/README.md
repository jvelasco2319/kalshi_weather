# Approved Probability Lab UI source

`preview.html` is the exact HTML supplied and approved by the user.

`approved_probability_lab_exact.html` is an identical named copy used by the implementation prompt.

Both files must have SHA-256:

```text
79d7fe8a7ab8e445a05f31c43e54f84b940969ab9a296da59614b3c0839c2018
```

The embedded July 7 data and browser-side formulas are demonstration fixtures. Codex must preserve the visual structure and interactions while replacing the fixture and decision-critical calculations with live immutable backend explainability snapshots.
